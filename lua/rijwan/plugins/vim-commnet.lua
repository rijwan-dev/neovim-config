-- This configuration sets up the 'vim-commentary' plugin for Neovim.
-- It provides an easy way to comment and uncomment lines.

return { 
    "tpope/vim-commentary",
    event = "VimEnter",

    --* key_mapping = "gcc",  -- Example useful option for setting a custom key mapping
}
